---
phase: 03-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified: []
autonomous: false
requirements:
  - DEP-01
  - DEP-02

must_haves:
  truths:
    - "App is live and accessible at starter.dotnetth.com"
    - "GitHub Actions workflow ran successfully on push to main"
    - "Query parameter URLs work correctly (no 404 on direct access)"
    - "Blazor app boots and generates projects at the public URL"
  artifacts: []
  key_links:
    - from: "GitHub Actions workflow"
      to: "gh-pages branch"
      via: "peaceiris deploy action"
      pattern: "gh-pages branch contains published wwwroot"
    - from: "starter.dotnetth.com DNS"
      to: "GitHub Pages"
      via: "CNAME record"
      pattern: "CNAME points to github.io"
---

<objective>
Verify the full deployment pipeline works end-to-end: GitHub Actions builds and deploys on push to main, the app loads at starter.dotnetth.com, and query parameter URLs work correctly.

Purpose: Confirm both DEP-01 (static hosting with SPA routing) and DEP-02 (CI/CD automation) are fully functional.
Output: Verified live deployment at starter.dotnetth.com.
</objective>

<execution_context>
@/Users/kidchenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kidchenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deployment/03-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure GitHub Pages and DNS</name>
  <files>N/A — human configuration only</files>
  <action>
  Plan 03-01 created the GitHub Actions workflow and all required static files. The code is ready to deploy but requires manual configuration steps that Claude cannot perform.

  **Step 1: DNS Configuration**
  At your domain registrar for `dotnetth.com`, add a CNAME record:
  - Type: CNAME
  - Name: starter
  - Value: kidchenko.github.io (replace with your actual GitHub username)
  - TTL: 3600 (or auto)

  DNS propagation may take up to 24 hours, but often completes within minutes.

  **Step 2: Push to main**
  Push the committed changes to the main branch. The GitHub Actions workflow will trigger automatically.

  **Step 3: Configure GitHub Pages in repository Settings**
  1. Go to repository Settings > Pages
  2. Source: "Deploy from a branch"
  3. Branch: `gh-pages` / `/ (root)`
  4. Custom domain: `starter.dotnetth.com`
  5. Check "Enforce HTTPS" (may take up to 24h for certificate provisioning after DNS propagates)

  **Step 4: Verify workflow succeeded**
  1. Go to repository Actions tab
  2. Confirm the "CI/CD" workflow ran successfully (green checkmark)
  3. Confirm both `build` and `deploy` jobs passed

  Resume signal: Type "deployed" when the workflow has succeeded and GitHub Pages is configured, or describe any issues.
  </action>
  <verify>
    <automated>gh run list --workflow=deploy.yml --limit=1 --json status,conclusion 2>/dev/null || echo "Check GitHub Actions tab manually"</automated>
  </verify>
  <done>DNS CNAME record configured, GitHub Pages settings configured with gh-pages branch source and custom domain, workflow ran successfully with green checkmark on both build and deploy jobs</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live deployment</name>
  <files>N/A — verification only</files>
  <action>
  The GitHub Actions pipeline deployed the Blazor WASM app to GitHub Pages with custom domain starter.dotnetth.com. Verify the live deployment works end-to-end:

  1. Visit https://starter.dotnetth.com — app should load with the dark-themed NetStarter UI
  2. Fill in some configuration options and click Generate — zip download should work
  3. Copy a shareable URL with query params (e.g., `https://starter.dotnetth.com/?type=console&sdk=net10`)
  4. Open that URL in a new tab — configuration should be restored from query params
  5. Open browser DevTools > Console — no integrity check errors or 404s for `_framework/` files
  6. Open browser DevTools > Network — confirm `_framework/blazor.webassembly.js` loads (200 status)

  Resume signal: Type "approved" if the app works correctly at the public URL, or describe any issues.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://starter.dotnetth.com/ 2>/dev/null || echo "Check manually in browser"</automated>
  </verify>
  <done>App loads at starter.dotnetth.com, Blazor boots without integrity errors, Generate+download works, query parameter URLs restore configuration correctly</done>
</task>

</tasks>

<verification>
1. GitHub Actions workflow shows green checkmark for the push-to-main run
2. gh-pages branch exists in the repository with published wwwroot content
3. App loads at https://starter.dotnetth.com without errors
4. Query parameter URLs work without 404s
5. Blazor app boots fully (not blank page — _framework/ is served correctly)
6. Generate + download works at the public URL
</verification>

<success_criteria>
The app is live and fully functional at starter.dotnetth.com. A push to main triggers automated build, test, and deploy. The user can share configuration URLs that work when opened directly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-deployment/03-02-SUMMARY.md`
</output>
